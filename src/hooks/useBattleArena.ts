// import { useDispatch, useSelector } from 'react-redux'
// import { selectFighter, startBattle } from '@/features/battle/battleSlice'

// export function useBattleArena() {
// 	const dispatch = useDispatch()
// 	const fighters = useSelector((state) => state.battle.fighters)
// 	const result = useSelector((state) => state.battle.result)
// 	return {
// 		fighters,
// 		result,
// 		selectFighter: (id: string) => dispatch(selectFighter(id)),
// 		startBattle: () => dispatch(startBattle()),
// 	}
// }
